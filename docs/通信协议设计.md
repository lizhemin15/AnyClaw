# AnyClaw 通信协议设计

本文档定义 anyclaw-web、anyclaw-api、anyclaw-server 之间的通信方式，供后续实现参考。

---

## 1. 整体数据流

```
用户浏览器 ←→ AnyClaw-Web ←→ AnyClaw-API ←→ AnyClaw-Server（容器）
              REST/WS         REST/WS          出站 WS
```

- **AnyClaw-Web ↔ AnyClaw-API**：REST（鉴权、实例管理）+ WebSocket（与 Claw 聊天）
- **AnyClaw-API ↔ AnyClaw-Server**：容器主动出站 WebSocket 连 API，API 做双向转发

---

## 2. 协议选型

### 2.1 聊天消息协议：Pico Protocol（复用现有）

AnyClaw-Server 已有 Pico Protocol，直接复用，无需新协议：

| 方向 | 类型 | 说明 |
|------|------|------|
| 客户端→服务端 | `message.send` | 用户发送文本 |
| 客户端→服务端 | `media.send` | 用户发送媒体（可选） |
| 客户端→服务端 | `ping` | 保活 |
| 服务端→客户端 | `message.create` | AI 回复文本 |
| 服务端→客户端 | `message.update` | 流式更新/占位符替换 |
| 服务端→客户端 | `media.create` | AI 回复媒体 |
| 服务端→客户端 | `typing.start` / `typing.stop` | 打字状态 |
| 服务端→客户端 | `pong` | 保活响应 |
| 服务端→客户端 | `error` | 错误 |

**JSON 帧格式**（与 pico 一致）：

```json
{
  "type": "message.send",
  "id": "uuid",
  "session_id": "xxx",
  "timestamp": 1234567890,
  "payload": { "content": "用户输入" }
}
```

---

## 3. AnyClaw-Web ↔ AnyClaw-API

### 3.1 REST API（鉴权与实例）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/auth/register` | 注册 |
| POST | `/auth/login` | 登录 |
| POST | `/auth/refresh` | 刷新 Token |
| GET | `/me` | 当前用户 |
| GET | `/instances` | 实例列表 |
| GET | `/instances/:id` | 实例详情 |
| POST | `/instances` | 创建实例（付费后） |
| DELETE | `/instances/:id` | 删除实例 |

### 3.2 WebSocket：与 Claw 聊天

**连接**：`wss://api.anyclaw.com/instances/:instance_id/ws`

**鉴权**：
- Header: `Authorization: Bearer <JWT>`
- 或 Cookie 携带 JWT（httpOnly）

**校验**：API 校验 JWT，确认 `instance_id` 属于当前用户后建立连接。

**消息**：使用 Pico Protocol JSON 帧，与 anyclaw-server 完全一致。API 只做透明转发，不解析 `type` 等业务字段。

---

## 4. AnyClaw-Server ↔ AnyClaw-API（出站桥接）

### 4.1 连接建立

**方向**：AnyClaw-Server 作为**客户端**主动连接 API。

**端点**：`wss://api.anyclaw.com/containers/connect`

**鉴权**（二选一或组合）：
- Query: `?instance_id=xxx&token=xxx`
- Header: `Authorization: Bearer <ANYCLAW_TOKEN>`
- 或首帧发送 `register` 类型的 JSON，包含 `instance_id`、`token`

**环境变量**（容器启动时注入）：
- `ANYCLAW_API_URL`：如 `wss://api.anyclaw.com`
- `ANYCLAW_INSTANCE_ID`：实例 ID
- `ANYCLAW_TOKEN`：实例级 token，API 校验后绑定连接

### 4.2 注册流程（若采用首帧注册）

1. 容器连接 `wss://api/containers/connect`
2. 首帧发送：
   ```json
   { "type": "register", "instance_id": "xxx", "token": "xxx" }
   ```
3. API 校验后回复：
   ```json
   { "type": "registered", "instance_id": "xxx" }
   ```
4. 之后所有帧按 Pico Protocol 透明转发

### 4.3 消息流

- **用户 → Claw**：Web 发 `message.send` → API 转发 → 容器收到，进入 Agent 处理
- **Claw → 用户**：容器发 `message.create` 等 → API 转发 → Web 展示

API 仅维护 `instance_id → 容器 WebSocket` 映射，按 instance 做双向转发，不解析 Pico 载荷。

---

## 5. AnyClaw-Server 侧改造要点

### 5.1 模式切换

| 模式 | 触发条件 | 行为 |
|------|----------|------|
| 入站（现有） | `channels.pico.enabled=true` 且无 `ANYCLAW_*` | 监听 `/pico/ws`，客户端连进来 |
| 出站桥接 | 设置 `ANYCLAW_API_URL` + `ANYCLAW_INSTANCE_ID` + `ANYCLAW_TOKEN` | 不监听端口，主动连 API WebSocket |

### 5.2 实现方式

- **方案 A**：新增 channel `anyclaw_bridge`，实现 `Channel` 接口，内部作为 WebSocket 客户端连 API，收到消息后走现有 `HandleMessage` 流程，发送时走 `Send` 等接口。
- **方案 B**：在 Pico channel 内增加「出站模式」分支：若检测到 `ANYCLAW_*` 环境变量，则启动出站连接逻辑，复用 Pico 的 `handleMessage`、`broadcastToSession` 等，但连接方向相反。

### 5.3 Session 映射

- 出站模式下，每个实例通常对应一个 session（`instance_id` 或固定值如 `default`）。
- chatID 建议：`anyclaw_bridge:{instance_id}` 或 `pico:{instance_id}`，与现有路由兼容。

---

## 6. 安全与重连

- **Token**：实例级 token 仅用于容器↔API，与用户 JWT 分离。
- **重连**：容器断线后应指数退避重连，API 将旧连接从映射中移除。
- **超时**：Ping/Pong 保活，超时断开并触发重连。

---

## 7. 总结

| 通信对 | 协议 | 说明 |
|--------|------|------|
| Web ↔ API | REST + WebSocket | REST 做鉴权与实例管理，WS 用 Pico 帧与 Claw 聊天 |
| API ↔ Server | 出站 WebSocket | 容器连 API，API 透明转发 Pico 帧 |
| 消息格式 | Pico Protocol | 复用现有 JSON 帧，无需新协议 |

**已实现**：AnyClaw-Server 中已新增 `anyclaw_bridge` channel，当环境变量 `ANYCLAW_API_URL`、`ANYCLAW_INSTANCE_ID`、`ANYCLAW_TOKEN` 均设置时自动启用，容器主动连接 API 的 `wss://{API_URL}/containers/connect`。
